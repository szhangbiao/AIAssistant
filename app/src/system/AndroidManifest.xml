<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:sharedUserId="android.uid.system"
    android:sharedUserMaxSdkVersion="28"
    tools:ignore="UnusedAttribute">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 开机自启动权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- 自定义权限：控制对WakeUpService的访问 -->
    <permission
        android:name="${applicationId}.permission.WAKE_UP_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:name=".AIApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIAssistant"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|touchscreen|screenLayout|orientation|screenSize|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AIAssistant.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.VoiceAssistantService"
            android:foregroundServiceType="mediaPlayback" />

        <!-- 兼容 Framework 显式指定的 Service 名字 -->
        <service
            android:name="com.booslink.tmallvoiceremotecontrol.service.VoiceService"
            android:exported="true"
            tools:ignore="ExportedService" />

        <!-- 根据遥控器媒体按键Down/UP手动控制录音 -->
        <service
            android:name=".service.WakeUpService"
            android:exported="true"
            android:permission="${applicationId}.permission.WAKE_UP_SERVICE">
            <intent-filter>
                <action android:name="cn.booslink.llm.WakeUpService" />
                <action android:name="com.booslink.aiui.voiceremote.RemoteControlService" />
            </intent-filter>
        </service>

        <!-- 开机自启动广播接收器 -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:directBootAware="true"
            android:enabled="true"
            android:exported="true"> <!-- 如果需要在解锁前运行，需加上这个 -->
            <intent-filter android:priority="1000"> <!-- 系统应用可以设置高优先级 -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="CHANNEL"
            android:value="${channelValue}" />
    </application>
</manifest>